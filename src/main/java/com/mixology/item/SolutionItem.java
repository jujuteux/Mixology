
package com.mixology.item;

import net.minecraft.item.Item;
import net.minecraftforge.fluids.UniversalBucket;

public class SolutionItem extends UniversalBucket {
	public SolutionItem() {
		super(
//				MixologyModFluids.SOLUTION, new Item.Properties().craftRemainder(Items.BUCKET).stacksTo(1).rarity(Rarity.COMMON)
		);
	}
}
